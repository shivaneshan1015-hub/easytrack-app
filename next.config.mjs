/** @type {import('next').NextKey} */
const nextConfig = {
  reactStrictMode: true,
  // This explicitly forces Next.js to prioritize files inside your /pages folder
  useFileSystemPublicRoutes: true,
};

export default nextConfig;